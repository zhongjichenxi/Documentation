# 不良资产AI尽调系统



## 一、这个项目是做什么的？

传统模式下，收购一个项目之前，尽职调查团队需要花**几周甚至几个月**收集工商、司法、合同、抵押物等信息，最后写成一份尽调报告。

这个软件的目标是：**把这几周压缩到半天**。

具体做法：
- **自动层**: 系统自动去查工商、法院、拍卖、舆情等公开数据
- **解析层**: 用户把借款合同、判决书、征信报告等PDF/扫描件上传，AI自动提取关键字段（本金、利率、抵押物地址、诉讼时效等）
- **人工层**: AI提取的结果不直接进报告，必须有人核对、修正、补录（比如现场勘查的照片、访谈录音）
- **报告层**: 最终自动生成一份符合金融合规要求的尽调报告

**核心原则**: 这不是一个「全自动机器人」，而是一个「AI增强型调查工作台」——AI做信息搬运和初稿，人做判断和确认。

---

## 二、系统定位：一个「数据密集型AI工作流引擎」

用一句话告诉工程师：

> 我们要搭的是一个**能同时处理「外部数据自动抓取」「海量PDF智能解析」「多人在线协作填数据」「最后拼成一份Word报告」**的工作流平台。

它很像一个「金融行业的项目管理+文档智能+在线协作」的混合体。

---

## 三、软件框架需要支持的五大模块

### 模块A：数据采集引擎（自动层）

**我们要什么：**
- 系统能自动对接多个外部数据源（企查查、天眼查、裁判文书网、执行信息公开网、阿里拍卖等）
- 一个项目里有10个关联主体（债务人、担保人、关联企业），能一键并发查询，不要一个个点
- 查询失败能自动重试，某个源挂了能切换备用源或提示用户
- 不同数据源返回矛盾信息时（比如A源说企业存续，B源说已破产），系统要标记冲突，让人工决定采信哪个
- 查回来的数据要缓存，24小时内同样查询直接读缓存

**现在不做，但架构要预留：**
- 未来会接银行内部数据、AMC私有接口、税务数据——所以数据源必须是「插件化」的，加一个源就像插一个U盘，不动核心代码

---

### 模块B：文档智能解析引擎（解析层）

**我们要什么：**
- 用户批量拖拽上传PDF/图片/Excel（一个资产包可能几百份文件）
- 上传后系统能自动识别「这是借款合同」「这是他项权证」「这是判决书」
- 扫描件里的文字要提取出来，变成结构化数据（本金多少、利率多少、抵押物在哪、期限多久）
- 提取出来的字段要打分：高置信自动确认，中置信标黄让人核对，低置信标红必须人工填
- 用户核对时，要能看到「左边是PDF原文，右边是AI提取的字段」，点击字段能定位到PDF具体位置
- 用户修改/确认字段后，系统记录谁改的、从什么改成什么

**现在不做，但架构要预留：**
- 未来要支持手写体、表格、盖章遮挡等更复杂的OCR场景
- 数据敏感，大客户会要求完全私有化部署，所以文档存储和解析流程不能绑死公有云

---

### 模块C：项目协作与人工补录（人工层）

**我们要什么：**
- 一个项目多人协作：业务经理、律师、会计师可能在不同时间参与
- 每个人看到的内容和操作权限不同（实习生只能录入，经理能确认估值，合伙人能定价）
- 尽调有标准清单（Checklist），不同资产类型清单不同（住宅抵押 vs 工业厂房），系统要自动加载对应清单
- 现场人员用手机拍照、录音，回来同步到项目里（照片要带时间地点水印）
- 任何信息块都能评论、@同事、标记疑点
- 所有操作留痕，满足金融审计要求

**现在不做，但架构要预留：**
- 未来要支持离线模式（地下室/工厂没信号时能拍照，有网后批量同步）
- 未来要支持外部律师/会计师接入，所以权限模型要足够细

---

### 模块D：AI Copilot 对话窗口

**我们要什么：**
- 用户在报告任何段落旁能点击「问AI」，AI基于这个项目的全部资料回答
- 用户能上传新文档继续追问（比如刚拿到一份租赁合同，让AI分析风险）
- 对话历史跟着项目走，不能丢，支持分主题（「估值讨论」「法律问题」）
- AI回答必须标注「这个结论来自哪份文档的第几页」

**现在不做，但架构要预留：**
- 这是RAG（检索增强生成）架构，需要向量数据库支持，MVP阶段数据量小可以先用PostgreSQL插件，但代码层要抽象好，方便后期换独立向量库

---

### 模块E：报告引擎（输出层）

**我们要什么：**
- 不同客户报告格式不同，系统要支持上传Word模板，模板里留变量（如`{{债权本金}}`）
- 报告里的数字不是死文字，而是联动数据库的——后台数据改了，报告里能刷新
- AI生成报告初稿后，人工在Web上修改，所有修改留痕
- 能导出Word（给客户二次编辑）和PDF（带水印，正式提交）
- 报告版本管理：每次修改存一个版本，能看改了什么

**现在不做，但架构要预留：**
- 未来要导出PPT、支持在线多人批注、支持电子签章

---

## 四、MVP第一阶段

**第一阶段只做这些：**
- 资产类型：债权类不良资产，有抵押物（住宅/商业优先）
- 核心闭环：创建项目 → 上传合同 → AI解析 → 人工核对 → 生成报告
- 数据源：工商、司法、拍卖、基础舆情、等等
- 报告格式：Word模板 + PDF导出
- 协作：基础权限 + 评论 + 审计日志

**第一阶段明确不做（但架构要预留接口）：**
- ❌ 复杂审批流（只保留「草稿→定稿」两态）
- ❌ 完整离线移动端（先只做拍照上传H5页面）
- ❌ 股权类资产（数据库预留字段，前端不开发）
- ❌ 投后监控预警（预留定时任务框架，不跑实际任务）
- ❌ 多格式模板（只做Word）

---

## 五、非功能性底线（架构设计时必须满足）

| 底线要求      | 一句话说明                                                 |
| --------- | ----------------------------------------------------- |
| **租户强隔离** | 我们是SaaS模式，A机构绝对不能看到B机构的数据。所有表必须有`tenant_id`，查询必须自动注入  |
| **私有化部署** | 银行/AMC不可能把数据上公有云。代码不能绑死阿里云/腾讯云独有服务，必须能Docker一键部署到客户机房 |
| **审计留痕**  | 谁改了哪个字段、从什么改成什么、什么时候改的，必须100%记录，不可删除，定期归档             |
| **异步处理**  | 查10个主体的工商司法信息、解析100页PDF，不可能同步等待。必须有任务队列+前端进度推送        |
| **人机降级**  | AI解析失败时，系统不能崩溃，必须无缝变成「待人工录入」状态                        |
| **数据溯源**  | 报告里的任何一个数字，必须能追溯到它来自哪份文档的哪一页、哪个API的哪次查询               |

---

## 要点

> "我们这个系统不是普通的企业后台，它是一个**AI增强型调查工作台**。用户是金融行业的调查人员，工作像侦探——收集碎片信息、交叉验证、现场勘查、最后写报告。
>
> 技术上有五个底线：
> 1. **数据可追溯**：报告里任何一个数字，必须能追溯到来源文档和API查询
> 2. **人机可切换**：AI失败时不崩溃，自动降级为人工录入
> 3. **租户强隔离**：多机构共用一套系统，数据绝不能串
> 4. **预留私有化**：代码不能绑死某一家云，要能部署到客户内网
> 5. **领域解耦**：项目、文档、数据、报告四个模块要独立，方便未来扩展投中、投后、退出等环节
>
> 第一阶段目标只有一个：**用户上传一个不良资产项目的全套档案，系统在10分钟内完成解析和初稿，人工用1小时核对修正后，能导出一篇符合内部标准的尽调报告。**"

---

> **附**: AI模型相关的工作（OCR、LLM字段提取、RAG对话）由另一位AI工程师负责，本次沟通聚焦软件框架、数据库设计、API层、部署架构。
